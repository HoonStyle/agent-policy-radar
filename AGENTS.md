# Agent Policy Radar 작업 지침

이 프로젝트는 모델/하네스/프로젝트 지침의 운영 정책을 기록하고 점검하는 별도 레이어다.

## 원칙

- 회사별 기본 하네스와 공식 동작을 우선한다. 추가 지침·설정 보정은 실제 환경에서 확인된 문제가 있을 때만 해당 범위에 최소한으로 제안한다. 다른 회사의 우회책을 그대로 이식하거나 모델 트렌드만으로 예방 규칙을 늘리지 않는다. 명시적인 사용자·프로젝트 요구와 안전 경계는 유지한다.

- 공식 문서에서 확인한 사실과 운영 해석을 분리한다.
- 다른 프로젝트(`tierwork`, `local-ai-connector`, `greplet` 등)의 파일을 이 프로젝트 작업 중 임의로 수정하지 않는다.
- 변경 제안은 `recommendations/`에 먼저 기록하고, 실제 적용은 사용자의 별도 승인 후 대상 프로젝트에서 수행한다.
- 모델 트렌드나 하네스 성향은 날짜를 붙여 기록한다. 시간이 지나면 낡을 수 있음을 명시한다.
- 대규모 벤치마크나 비용이 큰 실험은 자동으로 만들거나 실행하지 않는다.
- 목적은 “최신 모델/하네스 기준으로 지침이 과한지, 빠진 게 있는지, 분리해야 하는지”를 점검하는 것이다.

## 피드백·검증·업데이트 운영

- 문구 검토를 반복하며 규칙을 늘리기보다 실제 사용 피드백을 우선한다. 확인된 오류·근거 왜곡·안전 문제는 수정하고, 반복 불편은 원인 확인 후 최소 보완한다. 개인 취향은 공통 스킬보다 사용자·프로젝트 설정으로 다룬다. 피드백이나 새 트렌드만으로 자동 수정하지 않는다.
- 작은 수정은 변경 이유·수정 내용·검증 결과만 간단히 남긴다. 상세 ledger·독립 리뷰·새 승인 절차를 매번 요구하지 않는다. 변경·영향 범위를 검증하고, 관련 버전·조건이 같은 이전 근거는 재사용 사실을 밝힌다.
- CI/설치 검증과 실제 작업 품질 검증을 구분한다. 논문 스킬은 원문·인용·수치·해석·집필 결과로 검증하며, 간호학적·통계적 타당성이나 IRB 적합성을 CI 통과로 보증하지 않는다. 실제 환자/참여자 자료 대신 필요한 비식별 자료·합성 사례로 검증한다.
- 피드백 판단·수정·버전 결정은 사용자 요청에 따라 에이전트가 수행하고, 자동 자기수정/릴리즈로 설명하지 않는다. 릴리즈 요청 시 CHANGELOG와 관련 manifest 버전을 맞추고 v태그를 push한다. 태그의 macOS/Windows CI·패키징 검사 통과 후 GitHub Actions가 Release를 생성하도록 하며 수동 공개로 게이트를 우회하지 않는다.
- 별도 선택 플러그인은 자동 설치하지 않는다. 배포 완료와 로컬 설치 업데이트를 구분하고, 미검증 범위·실패를 사실대로 보고한다.

## 기록 형식

- `sources/`: 공식 문서 요약, URL, 확인 날짜
- `findings/`: 실제 사용/관찰 기반 판단, 근거, 한계
- `recommendations/`: 프로젝트별 제안과 적용 전 확인사항
- `notes/`: 대화에서 나온 원문성 메모와 결정 기록
