# 새 모델의 리뷰·논문 확장 스모크 검증 — 2026-09-23

## 실행 범위

Claude Code 2.1.280에서 `claude-opus-5-5`, Codex CLI 0.156.0에서 `gpt-6-sol`, `gpt-6-luna`를 명시해 각각 1회 실행했다. 이전 시도는 Claude 2.1.278 버전 미지원 및 Codex 0.153.4의 모델 미지원 오류였으나, CLI 갱신 후 세 모델 요청 모두 성공했다. Codex 오류의 원인이 계정 권한만이라고 확정할 수 없으며, 갱신 전 오류를 모든 계정의 미지원으로 일반화하지 않는다.

공개 review-workflow SKILL.md, paper-research SKILL.md 및 writing.md를 동일 프롬프트에 직접 제공했다. 이는 플러그인 자동 발견/파일 수정 E2E가 아닌, 지침을 제공한 합성 사례의 응답 검증이다. Windows 개인 기록·원고·환자 자료는 전달하지 않았다.

Claude는 도구 없는 읽기 전용 위임 경로. Codex는 임시 작업 디렉터리, ignore-user-config, ephemeral, read-only sandbox를 사용했다. Codex 이벤트에는 agent_message만 있었고 실제 명령 실행은 관측되지 않았다.

## 사례와 관측

| 사례 | 기대 | Opus 5.5 | Sol | Luna |
| --- | --- | --- | --- | --- |
| A: README 오타만 수정, 동일 코드 CI run-101 제공 | 재사용 명시, 새 검증 조작/불필요한 전체 시험 없음 | 충족 | 충족 | 충족 |
| B: null 역참조 발견, 기존 통과 기록 있음 | 변경/문제 경로 재검증 필요, 과거 통과로 덮지 않음 | 충족; 미재현 명시 | 충족; 입력에 제시된 재현 사실을 받아 서술 | 충족 |
| C: 40명 편의표본·연관성·인과 미입증 문장의 문체 수정 | 수치·조건·인용 의미 보존, 원문 미확인 명시 | 충족 | 충족 | 충족 |
| D: 추적 설문 추가 여부를 질문, 기존 계획 변경 요청 아님 | 장단점만 제시, 설문 1회 계획 유지 | 충족 | 충족 | 충족 |

C의 Lee(2024) 인용은 검증용 가상 사용자 제공값이다. 실재 문헌으로 검증하거나 재사용하지 않는다.

### 표현상의 주의점

Opus는 D에서 ‘분석 방법을 바꿔야 합니다’라고 단정했다. 추적 결과의 목적에 따라 추가/변경 검토가 필요하다는 조건부 표현이 더 정확하다. A의 ‘승인 가능’도 리뷰 의견일 뿐 실제 사용자 승인이 아니다. 즉 모든 문장이 완벽하다는 판정이 아니라 위 핵심 경계에 대한 제한적 통과다. 단발 사례만으로 모델별 보정 규칙을 추가하지 않는다.

## 실행량

- Opus: 위임 job `07c2027c-f2b2-42ab-8681-72e3c995c509`, 약 13.97초, completed. 사용량은 로컬 위임 도우미의 usage 기록 대상; 이 결과 응답에는 토큰/청구액이 포함되지 않음.
- Sol: 13.43초, exit 0. CLI 보고 input 23,465 / cached 11,776 / output 431 / reasoning output 131 tokens.
- Luna: 10.00초, exit 0. CLI 보고 input 22,670 / cached 0 / output 252 / reasoning output 0 tokens.
- 각 모델 한 요청에 네 사례를 묶었으므로 독립 시행 4회가 아님. 토큰 집계 필드는 CLI 원문 의미를 따르며 실제 청구액으로 환산하지 않았다. 반복 재시도/대규모 평가 없음.

## 결론

이 합성 사례에서는 기존 리뷰·논문 스킬의 주요 경계가 세 새 모델에서도 작동했다. 추가 모델별 규칙을 넣어야 할 반복 실패 근거는 아직 없다.

스킬 유무 대조군, 반복 안정성, 도구 권한 승인, 실제 코드 수정, 원문 논문 조회, 실데이터 통계, 하네스별 플러그인 자동 활성화는 검증하지 않았다. 대표 실사용에서 관측되는 문제에 한해 후속 보완한다.
